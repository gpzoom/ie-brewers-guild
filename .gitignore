# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Supabase CLI local state
supabase/.temp

# Subagent-driven-development scratch workspace (ledgers, briefs, review
# packages) -- git history is the record once a plan's workspace is deleted.
.superpowers/

# npm is the maintained package manager (package-lock.json); bun.lock is not
# kept in sync and its presence makes Cloudflare's build auto-detection pick
# bun over npm, which then fails frozen-lockfile installs.
bun.lock
bun.lockb

# Local env (Google Maps API keys, etc.)
.env
.env.local
.env.*.local
*.env
!.env.example

# Output of one-time geocode script — review then merge into src/data/site.ts
scripts/geocoded-members.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
